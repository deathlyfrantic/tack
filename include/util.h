#ifndef TACK_UTIL_H
#define TACK_UTIL_H

#include <stdint.h>
#include <stdlib.h>

#define MIN(a, b) (((a) < (b)) ? (a) : (b))

uint8_t get_num_strlen(size_t);

#endif /* end of include guard: TACK_UTIL_H */
